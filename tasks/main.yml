---
- name: Validate service contract
  include_role:
    name: common.validate_schema

- name: Validate Samba workgroup name
  assert:
    that:
      - samba_workgroup is string
      - samba_workgroup | length > 0
      - samba_workgroup | regex_search('^[A-Z0-9_]{1,15}$')
      - samba_workgroup | upper == samba_workgroup
      - samba_workgroup not in samba_reserved_workgroups
    fail_msg: "samba_workgroup must be uppercase alphanumeric (max 15 chars) and not a reserved domain"

- name: Validate Samba user definitions
  assert:
    that:
      - item.name is defined
      - item.password is defined
      - item.uid is defined
      - item.gid is defined
      - item.password | length > 0
    fail_msg: "Each Samba user requires name, password, uid, and gid"
  loop: "{{ samba_users }}"
  loop_control:
    label: "{{ item.name | default('unnamed') }}"

- name: Validate Samba share definitions
  assert:
    that:
      - item.name is defined
      - item.path is defined
      - item.path | regex_search('^/')
    fail_msg: "Each Samba share requires an absolute path"
  loop: "{{ samba_shares }}"
  loop_control:
    label: "{{ item.name | default('unnamed') }}"

- name: Ensure Samba share names are unique
  assert:
    that:
      - (samba_shares | map(attribute='name') | list | unique | length) == (samba_shares | length)
    fail_msg: "Duplicate share names detected in samba_shares"

- name: Determine target runtimes
  vars:
    candidate_runtimes: "{{ runtime | default(target_runtimes | default(runtime_templates.keys() | list)) }}"
  set_fact:
    service_target_runtimes: "{{ candidate_runtimes if (candidate_runtimes is iterable and candidate_runtimes is not string) else [candidate_runtimes] }}"

- name: Set infrastructure template root
  set_fact:
    infrastructure_template_root: "{{ [role_path | dirname, 'infrastructure', 'templates'] | path_join }}"

- name: Build runtime template map
  set_fact:
    service_runtime_templates: "{{ service_runtime_templates | default({}) | combine({ item.key: ([infrastructure_template_root, item.value] | path_join) }) }}"
  loop: "{{ runtime_templates | dict2items }}"
  loop_control:
    label: "{{ item.key }}"

- name: Ensure requested runtimes are supported
  assert:
    that:
      - runtime_item in service_runtime_templates
    fail_msg: "Runtime {{ runtime_item }} not supported by service {{ service_id }}"
  loop: "{{ service_target_runtimes }}"
  loop_control:
    loop_var: runtime_item

- name: Render and apply runtime(s)
  loop: "{{ service_target_runtimes }}"
  loop_control:
    loop_var: runtime_item
    label: "{{ runtime_item }}"
  block:
    - name: Render runtime {{ runtime_item }}
      include_role:
        name: common.render_runtime
      vars:
        runtime_templates: "{{ service_runtime_templates }}"
        runtime: "{{ runtime_item }}"

    - name: Apply runtime {{ runtime_item }}
      include_role:
        name: common.apply_runtime
      vars:
        runtime: "{{ runtime_item }}"

- name: Ensure exports directory exists
  vars:
    exports_env_dir: "{{ service_exports_env_dir | default([playbook_dir | default('.'), 'exports'] | path_join) }}"
  delegate_to: localhost
  run_once: true
  become: false
  file:
    path: "{{ exports_env_dir }}"
    state: directory
    mode: "0755"
  when: "exports is defined and (exports.env | default([])) | length > 0"

- name: Write exports environment file
  vars:
    exports_env_dir: "{{ service_exports_env_dir | default([playbook_dir | default('.'), 'exports'] | path_join) }}"
    exports_env_file: "{{ service_exports_env_file | default([exports_env_dir, service_id ~ '.env'] | path_join) }}"
    exports_env_entries: "{{ exports.env | default([]) }}"
  delegate_to: localhost
  run_once: true
  become: false
  copy:
    dest: "{{ exports_env_file }}"
    mode: "0640"
    content: |-
      {% for item in exports_env_entries %}
      {{ item.name }}={{ item.value }}
      {% endfor %}
  when: "exports_env_entries | length > 0"
